# ── Python / Django ────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
venv/
env/
.venv/

# Secretos — NUNCA versionar. Ver Docs/PLAN_PASO_A_PASO.md P0.3
.env
.env.local
*.env
!.env.example

# Base de datos local
*.sqlite3
*.sqlite3-journal

# Archivos subidos por los usuarios (P9 de la biblia: el archivo va al
# disco, a la base va el registro. Ninguno de los dos va a git)
media/
Backend/media/
repositorio/
Backend/repositorio/

# ── REGLA DE ORO: nada sensible ni de otros proyectos ──────────
# Nada que sea secreto, peligroso, de un cliente, o material de otro
# proyecto entra a este repositorio. Si hay duda, NO se sube.
# Se revisa `git status` antes de cada commit.
#
# Este repositorio contiene UNICAMENTE codigo fuente.
# Decidido el 2026-08-02 por el autor.

# TODA la documentacion queda fuera: biblia, features, plan, avance,
# bitacora, y el material heredado de otros proyectos (MILA, Copiloto
# de Revit). Vive solo en el disco del autor.
Docs/
curva/

# Documentos con datos reales de una empresa o de personas
*confidencial*
*credenciales*
*password*
*.pem
*.key
*.p12
*.pfx

# ── Node / Vue ─────────────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Editores ───────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# ── Sistema operativo ──────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# ── Temporales ─────────────────────────────────────────────────
*.tmp
*.bak
~$*
