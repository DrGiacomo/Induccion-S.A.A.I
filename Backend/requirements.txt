# ═══════════════════════════════════════════════════════════════
#  S.A.A.I — Dependencias del backend
# ═══════════════════════════════════════════════════════════════
#  Actualizado en P0.4 el 2026-08-02
#  Python 3.14.4  ·  PostgreSQL 17
#
#  Instalar:
#      python -m venv venv
#      venv\Scripts\activate
#      pip install -r requirements.txt
#
#  Regenerar tras instalar algo nuevo:
#      pip freeze > requirements.txt
#      (y volver a poner estos comentarios)
# ═══════════════════════════════════════════════════════════════


# ── Núcleo ─────────────────────────────────────────────────────
Django==6.0.5
djangorestframework==3.17.1
django-cors-headers==4.9.0

# Lee la configuración desde el archivo .env (paso P0.3). Sin esto el
# proyecto no arranca: settings.py ya no contiene ningún valor propio.
django-environ==0.14.0


# ── Base de datos ──────────────────────────────────────────────
# PostgreSQL, decisión D1. Es tecnología constitutiva del proyecto: la
# búsqueda con stemming en español y la tolerancia a errores de tipeo
# dependen de capacidades suyas que no son portables a otro motor.
#
# `psycopg[binary]` trae las librerías compiladas: no hace falta tener
# un compilador de C ni las cabeceras de PostgreSQL en la máquina.
psycopg[binary]==3.3.4

# Retirados en P0.4, cuando murió MySQL:
#   mysqlclient, PyMySQL  → los dos drivers que se pisaban entre sí, más
#                           el parche `pymysql.install_as_MySQLdb()` de
#                           manage.py que hacía que por manage.py corriera
#                           uno y por WSGI el otro.
#
# Retirado en P0.4, adelantando P1.2:
#   djangorestframework_simplejwt → declaraba JWT como autenticación por
#                           defecto sin tener un solo endpoint que emitiera
#                           tokens. Se decidió sesiones con cookie HttpOnly
#                           (decisión D2).


# ── Transitivas ────────────────────────────────────────────────
# No se importan directamente; las arrastran las de arriba. Se fijan
# para que una instalación futura reproduzca exactamente este entorno.
asgiref==3.11.1
cffi==2.0.0
cryptography==48.0.0
psycopg-binary==3.3.4
pycparser==3.0
PyJWT==2.12.1
sqlparse==0.5.5
tzdata==2026.2
