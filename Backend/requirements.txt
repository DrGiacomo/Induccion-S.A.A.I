# ═══════════════════════════════════════════════════════════════
#  S.A.A.I — Dependencias del backend
# ═══════════════════════════════════════════════════════════════
#  Generado en el paso P0.2 el 2026-08-02
#  Python 3.14.4
#
#  Instalar:
#      python -m venv venv
#      venv\Scripts\activate
#      pip install -r requirements.txt
#
#  Regenerar tras instalar algo nuevo:
#      pip freeze > requirements.txt
#      (y volver a poner estos comentarios)
# ═══════════════════════════════════════════════════════════════


# ── Núcleo ─────────────────────────────────────────────────────
Django==6.0.5
djangorestframework==3.17.1
django-cors-headers==4.9.0

# Lee la configuración desde el archivo .env (paso P0.3). Sin esto el
# proyecto no arranca: settings.py ya no contiene ningún valor propio.
django-environ==0.14.0


# ── Base de datos ──────────────────────────────────────────────
# ⚠️ TEMPORAL — los tres desaparecen en P0.4 (migración a PostgreSQL,
#    decisión D1). Entonces entra `psycopg[binary]`.
#
# ⚠️ Hay DOS drivers de MySQL instalados a la vez:
#      - mysqlclient  → el MySQLdb real, compilado, el que debería usarse
#      - PyMySQL      → un sustituto en Python puro
#    Y `manage.py:5-6` llama a `pymysql.install_as_MySQLdb()`, que hace
#    que PyMySQL suplante a mysqlclient. Resultado: por manage.py corre
#    PyMySQL, pero por WSGI/ASGI correría mysqlclient. Driver distinto en
#    desarrollo y en producción, que es una fuente clásica de fallos
#    sutiles. Se resuelve solo al pasar a PostgreSQL.
mysqlclient==2.2.8
PyMySQL==1.1.3
cryptography==48.0.0


# ── Autenticación ──────────────────────────────────────────────
# ⚠️ TEMPORAL — se retira en P1.2. Está declarado en settings.py como
#    clase de autenticación por defecto pero NO tiene endpoints de token,
#    así que hoy no autentica a nadie. Se decidió sesiones con cookie
#    HttpOnly en su lugar (decisión D2).
djangorestframework_simplejwt==5.5.1


# ── Transitivas ────────────────────────────────────────────────
# No se importan directamente; las arrastran las de arriba. Se fijan
# para que una instalación futura reproduzca exactamente este entorno.
asgiref==3.11.1
cffi==2.0.0
pycparser==3.0
PyJWT==2.12.1
sqlparse==0.5.5
tzdata==2026.2
